'use strict';

/**
 * enabled-language controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::enabled-language.enabled-language');
